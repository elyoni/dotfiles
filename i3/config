# This file has been auto-generated by i2-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
#exec xmodmap -e "keycode 66 = Shift_L NoSymbol Shift_L"
exec xmodmap -e "keycode 66 = Escape NoSymbol Escape"

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
#font pango:DejaVu Sans Mono Book 11
#font pango:Ubuntu Mono Regular 13

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.


exec "sudo chmod o+rw /dev/ttyUSB1"
exec "stty -F /dev/ttyUSB1 115200 raw -echo -echoe -echok -echoctl -echoke"

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal

set $docker_terminal Launch: [0-6] Docker
bindsym $mod+Ctrl+Return mode "$docker_terminal"
mode "$docker_terminal" {
    bindsym Return exec "i3-sensible-terminal -e boxnew -s local -n 0", mode "default"
    bindsym 1 exec      "i3-sensible-terminal -e boxnew -s local -n 1", mode "default"
    bindsym 2 exec      "i3-sensible-terminal -e boxnew -s local -n 2", mode "default"
    bindsym 3 exec      "i3-sensible-terminal -e boxnew -s local -n 3", mode "default"
    bindsym 4 exec      "i3-sensible-terminal -e boxnew -s local -n 4", mode "default"
    bindsym 5 exec      "i3-sensible-terminal -e boxnew -s local -n 5", mode "default"
    bindsym 6 exec      "i3-sensible-terminal -e boxnew -s local -n 6", mode "default"

    bindsym Escape mode "default"
    bindsym Return mode "default"
}

bindsym $mod+Return exec i3-sensible-terminal
set $ssh_terminal Launch: [Enter]yoni [a]di [j]enk val[2-5] [d]ocker
bindsym $mod+Shift+Return mode "$ssh_terminal"
mode "$ssh_terminal" {
    bindsym Return exec "i3-sensible-terminal -e ssh -R 2222:localhost:22 yoni@yoni-pc -y", mode "default"
    bindsym a exec "i3-sensible-terminal -e ssh adi@adi -y", mode "default"
    bindsym j exec "i3-sensible-terminal -e ssh root@emb-jenk-slv01 -y", mode "default"
    bindsym 2 exec "i3-sensible-terminal -e ssh portia-linux-val02@portia-linux-val02 -y", mode "default"
    bindsym 3 exec "i3-sensible-terminal -e ssh se@portia-linux-val03 -y", mode "default"
    bindsym 4 exec "i3-sensible-terminal -e ssh se@portia-linux-val04 -y", mode "default"
    bindsym 5 exec "i3-sensible-terminal -e ssh se@portia-linux-val05 -y", mode "default"
    bindsym d exec "i3-sensible-terminal -e ssh devbox@localhost -p 221 -y", mode "default"

    bindsym Escape mode "default"
    bindsym Return mode "default"
}

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
bindsym $mod+Shift+d exec j4-dmenu-desktop
bindsym $mod+d exec xfce4-appfinder --class floatingWin
for_window [class="^floatingWin$"] floating enable

bindsym $mod+m move workspace to output left
bindsym $mod+Shift+m move workspace to output down


# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent


bindsym $mod+shift+ctrl+o exec echo '+' > /dev/ttyUSB0
bindsym $mod+shift+ctrl+p exec echo '-' > /dev/ttyUSB0


# focus the child container
#bindsym $mod+d focus child

set $workspace01 "1:"
set $workspace02 "2:  "
set $workspace03 "3:"
set $workspace04 "4:DoubleCMD"
set $workspace05 "F2:Lab/Tools"
set $workspace06 "6:Mail"
set $workspace07 "7: Windows"
set $workspace08 "8:My Log"
set $workspace09 "9:I3 "
set $workspace00 "0:Music"
set $workspace11 "F1:"
set $workspace21 "F11:MY"
set $workspace22 "F10:MY"

# switch to workspace
bindsym $mod+1 workspace $workspace01
bindsym $mod+2 workspace $workspace02
bindsym $mod+3 workspace $workspace03
bindsym $mod+4 workspace $workspace04
bindsym $mod+F2 workspace $workspace05
bindsym $mod+6 workspace $workspace06
bindsym $mod+7 workspace $workspace07
bindsym $mod+8 workspace $workspace08
bindsym $mod+9 workspace $workspace09
bindsym $mod+0 workspace $workspace00
bindsym $mod+F1 workspace $workspace11
bindsym $mod+F11 workspace $workspace21
bindsym $mod+F10 workspace $workspace22

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace01
bindsym $mod+Shift+2 move container to workspace $workspace02
bindsym $mod+Shift+3 move container to workspace $workspace03
bindsym $mod+Shift+4 move container to workspace $workspace04
bindsym $mod+Shift+F2 move container to workspace $workspace05
bindsym $mod+Shift+6 move container to workspace $workspace06
bindsym $mod+Shift+7 move container to workspace $workspace07
bindsym $mod+Shift+8 move container to workspace $workspace08
bindsym $mod+Shift+9 move container to workspace $workspace09
bindsym $mod+Shift+0 move container to workspace $workspace00
bindsym $mod+Shift+F1 move container to workspace $workspace11
bindsym $mod+Shift+F11 move container to workspace $workspace21
bindsym $mod+Shift+F10 move container to workspace $workspace22

#Assing program to workspace
## To check the click run xprop
assign [class="Firefox"] $workspace02
assign [class="Chromium-browser"] $workspace02
assign [class="Doublecmd"] $workspace04
assign [class="Vncviewer"] $workspace07
assign [class="Remmina"] $workspace07
assign [class="Thunderbird"] $workspace06
assign [class="Evolution"] $workspace06

### Workspace 0
for_window [class="Spotify"] move to workspace $workspace00
assign [class="Telegram"] $workspace00
assign [class="Slack"] $workspace00
assign [class="zoom"] $workspace00

#exec --no-startup-id i3-msg 'workspace 8:My Log; exec nvim /media/Data/my_log.md'

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3blocks
#        status_command i3status
        tray_output primary
        font xft:DejaVu Sans Mono Book 11
        #Droid Sans Mono, FontAwesome 10
}


exec "setxkbmap -option grp:switch,grp:alt_shift_toggle,grp_led:scroll us,il"
#exec "setxkbmap -layout us,il"
#exec "setxkbmap -option 'grp:alt_shift_toggle'"
#exec "setxkbmap -layout us"

bindsym --whole-window $mod+button2 kill
bindsym button3 floating toggle


# Press $mod+o followed by either f, t, Escape or Return to launch firefox,
# thunderbird or return to the default mode, respectively.
set $mode_launcher Launch: [f]irefox [t]hunderbird [d]oubleCommander [v]nc Viewer [r]dp [s]potify
bindsym $mod+o mode "$mode_launcher"

mode "$mode_launcher" {
    bindsym f exec firefox
    bindsym m exec evolution
    bindsym d exec doublecmd
    bindsym r exec "i3-sensible-terminal -e remmina -c ~/.remmina/1536472353509.remmina", mode "default"
    bindsym v exec vncviewer YEHONATANEL-PC
    bindsym s exec spotify 

    bindsym Escape mode "default"
    bindsym Return mode "default"
}




set $xrander_menu Launch: [1]StandAlone [2]Work [3]Home
bindsym $mod+F8 mode "$xrander_menu"
#--off --output DP-1-3 --off --output HDMI-1 --off --output DP-2 --off

mode "$xrander_menu" {
    bindsym 1 exec "xrandr --output HDMI-1 --off --output DP-1 --off --output DP-2 --off --output DP-1-3 --off --output DP-1-1 --off --output DP-1-2 --off  --output eDP-1 --auto ", mode "default"
    bindsym 2 exec "xrandr --output eDP-1 --off --output HDMI-1 --pos 3840x0 --auto --output DP-1-1 --pos 1920x0 --primary --auto --output DP-1-2 --pos 0x0 --auto", mode "default"
    #bindsym 2 exec "xrandr --output DP-2 --pos 3840x0 --auto --output DP-1-1 --pos 1920x0 --auto --output DP-1-2 --pos 0x0 --auto  --output eDP-1 --off ", mode "default"
    bindsym 3 exec "xrandr --output eDP-1 --pos 3296x1200 --auto --output HDMI-1 --pos 1920x120 --auto --output DP-1 --primary --pos 0x0 --auto ", mode "default"
    #bindsym 4 exec "xrandr --output eDP-1 --off --output HDMI-1 --pos 3840x0 --auto --output DP-1-1 --pos 1920x0 --auto --output DP-1-2 --pos 0x0 --auto", mode "default"


    bindsym Escape mode "default"
    bindsym Return mode "default"
}

#exec_always bash $HOME/.config/i3/scripts/xrander_script.sh
#exec_always bash $HOME/.dotfiles/i3/scripts/wallpaper_change.sh
#exec bash $HOME/.time_xprintidle

# Background color
exec --no-startup-id xsetroot -solid "#000000"

# Screenshot. Using flameshoot
bindsym --release Print exec "flameshot gui"
bindsym --release Shift+Print exec "flameshot full -c && notify-send Flameshot 'Full picture has been taken, saved in clipboard'"


# Set Status Bar Icons
## Add wifi manager only if there is a wifi module
#exec --no-startup-id ifconfig | grep wlp && nm-applet
exec --no-startup-id nm-applet
## Add volume manager
#exec --no-startup-id pasystray

bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks
bindsym --release Num_Lock  exec pkill -SIGRTMIN+11 i3blocks
bindsym --release Scroll_Lock exec pkill -SIGRTMIN+11 i3blocks

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec bash $HOME/.config/i3/scripts/volume.sh volume_up #increase sound volumeh
bindsym XF86AudioLowerVolume exec bash $HOME/.config/i3/scripts/volume.sh volume_down #decrease sound volume
bindsym XF86AudioMute        exec bash $HOME/.config/i3/scripts/volume.sh volume_mute

bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

#bindsym $mod+Shift+F12 exec sh /home/johnny/.config/i3/scripts/volume_up.sh
#bindsym $mod+Shift+F11 exec sh /home/johnny/.config/i3/scripts/volume_down.sh
#bindsym $mod+Shift+F10 exec sh /home/johnny/.config/i3/scripts/volume_mute.sh

#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% ; exec pkill -RTMIN+10 i3blocks #increase sound volume
#bindsym $mod+Shift+F11 exec --no-startup-id pactl set-sink-volume 1 -5% ; exec pkill -RTMIN+10 i3blocks #decrease sound volume
#exec hostname -s | grep johnny-PC && bindsym $mod+Shift+F12 exec --no-startup-id pactl set-sink-mute 1 toggle # mute sound

#Set wallpaper
#exec_always feh --bg-fill /home/yehonatan.e/Pictures/wallpaper.JPG
#exec_always feh --bg-fill $HOME/Pictures/wallpaper_blur_new.JPG

exec --no-startup-id xset dpms 600
exec --no-startup-id xautolock -time 1 -locker i3lock


#convert wallpaper_blur.JPG -blur 0x20 wallpaper_blur_new.JPG 
bindsym $mod+Ctrl+l exec bash $HOME/.dotfiles/i3/scripts/i3lock.sh
#bindsym $mod+Ctrl+l exec i3lock -i $HOME/Pictures/wallpaper.png
#bindsym $mod+Ctrl+l exec i3lock -c 000000


#Alias
#exec --no-startup-id alias plant="python3 ~/.dotfiles/work_script/serial_connection.py"
bindsym $mod+u exec python3 $HOME/.dotfiles/work_script/serial_connection.py -u
bindsym $mod+ctrl+U exec python3 $HOME/.dotfiles/work_script/serial_connection.py -d


bindsym $mod+F12 exec bash $HOME/.dotfiles/i3/scripts/touchpad_on_off.sh
# Sreen brightness controls
bindsym XF86MonBrightnessUp exec bash $HOME/.dotfiles/i3/scripts/backlight_brightness.sh +4 # increase screen brightness
bindsym XF86MonBrightnessDown exec bash $HOME/.dotfiles/i3/scripts/backlight_brightness.sh -4 # decrease screen brightness

#exec --no-startup-id slack 
exec --no-startup-id teams
exec --no-startup-id zoom
exec --no-startup-id davmail
exec --no-startup-id ~/apps/Telegram/Telegram
#exec --no-startup-id thunderbird
exec --no-startup-id workrave
exec --no-startup-id evolution
exec --no-startup-id autorandr -c
#exec --no-startup-id xfce4-panel --disable-wm-check
exec --no-startup-id xfce4-power-manager --disable-wm-check
#exec --no-startup-id xfce4-session
